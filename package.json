{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^16.11.1",
    "lerna": "^4.0.0",
    "ts-node": "^10.3.0",
    "typescript": "^4.4.4"
  },
  "scripts": {
    "start:admin": "lerna run --scope admin start",
    "dev:admin": "lerna run --scope admin dev",
    "start:mobile": "lerna run --scope mobile start",
    "dev:mobile": "lerna run --scope mobile dev",
    "start:scraper": "lerna run --scope scraper start",
    "dev:scraper": "lerna run --scope scraper dev",
    "start:server": "lerna run --scope server start",
    "dev:server": "lerna run --scope server dev --stream"
  }
}
