import { Configutation } from "./types";

const configuration: Configutation = {
  server: {
    host: "",
    port: 0,
  },
  db: {
    type: "mysql",
    host: "",
    username: "",
    password: "",
    port: 0,
    database: "",
  },
};

export default configuration;
